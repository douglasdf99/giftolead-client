name: saveleads-client-frontend

containerPort: 8080
cpu: 1
memory: 2048M
replicaCount: 2
maxSurge: 2

healthCheckPath: /

lb:
  type: nginx
  hosts:
    - host: extensions.poc.buildstaging.com
      paths: ["/"]

infra:
  role:
    irsa: true